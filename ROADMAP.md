# Production Readiness Roadmap

Status tracker for the 52 features identified in the launch-readiness review.

Legend: ✅ done · 🟡 in progress · ⬜ not started · ⏭ deferred / out of scope

---

## 🔴 Must-have before any paying customer

| #   | Feature                                                         | Status | Notes                                                                                                                                                                                                                                   |
| --- | --------------------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | User management (invite, list, role, disable, reset password)   | ✅     | `/api/users` + `/users` dashboard page. Roles: OWNER / ADMIN / AGENT / VIEWER.                                                                                                                                                          |
| 2   | Self-service signup + tenant creation                           | ✅     | `POST /auth/register` creates tenant + OWNER + JWT in one call; `/signup` page. Verification email sent when `EMAIL_VERIFICATION_REQUIRED=true`.                                                                                          |
| 3   | Forgot password (email token) + change own password             | ✅     | `POST /auth/forgot-password` (always 202 to prevent enumeration) emails a reset link. `POST /auth/reset-password` consumes a SHA-256-hashed, single-use token. 60-min TTL. `/forgot-password` and `/reset-password` pages in dashboard. |
| 4   | Per-tenant integration credentials (WhatsApp, Telegram, OpenAI) | ✅     | Tokens stored AES-GCM encrypted on `tenant_settings`. Real clients resolve per-tenant creds with env-var fallback. Settings UI is write-only (`*Configured` booleans on read).                                                            |
| 5   | Secrets at rest encryption (AES-GCM)                            | ✅     | `SecretCipher` (AES-GCM-256, random IV, AAD-tagged). Master key from `APP_ENCRYPTION_KEY`; dev fallback derived from JWT secret. 5 cipher tests.                                                                                          |
| 6   | Email verification on signup                                    | ✅     | Signed token via SMTP. `POST /auth/verify-email` + `POST /auth/resend-verification`. Gated by `EMAIL_VERIFICATION_REQUIRED` (off in dev, on in prod). `/verify-email` page in dashboard.                                                  |
| 7   | Rate limit on `/auth/login`                                     | ✅     | `LoginRateLimitFilter` — Redis fixed-window per IP + per email, default 10/min, returns 429. Fail-open if Redis is down. Configurable via `LOGIN_RPM`.                                                                                  |
| 8   | Account lockout after N failed logins                           | ✅     | `failed_login_count` + `locked_until` on users. Default 5 failures → 15-minute lock (configurable via `LOGIN_LOCK_THRESHOLD` / `LOGIN_LOCK_MINUTES`).                                                                                    |
| 9   | Error tracking (Sentry / GlitchTip)                             | ✅     | `sentry-spring-boot-starter-jakarta` + `sentry-logback` on backend; `@sentry/nextjs` on frontend. Inert until `SENTRY_DSN` / `NEXT_PUBLIC_SENTRY_DSN` is set. `SentryConfig.BeforeSendCallback` strips request bodies + cookies and tags events with `tenant_id`. |
| 10  | Automated nightly Postgres backups to S3                        | ✅     | `backup` service in `docker-compose.prod.yml` (`deploy/backup/`): pg_dump → gzip → S3, configurable cron + retention.                                                                                                                   |
| 11  | Readiness/liveness split + dependency health checks             | ✅     | `management.endpoint.health.probes.enabled=true` exposes `/actuator/health/{liveness,readiness}`. Custom Redis PING indicator + per-component `/actuator/health/{db,redis}`.                                                              |
| 12  | TOS / Privacy / Cookie banner                                   | ✅     | `/legal/terms` + `/legal/privacy` pages (placeholders for company/jurisdiction). `CookieBanner` component (essential-only vs accept-all, stored in localStorage). Signup gates submit on accepting Terms + Privacy. Footer links from login.                    |
| 13  | Data export per user request (GDPR Art. 15)                     | ✅     | CSV export at `/api/leads/export`.                                                                                                                                                                                                      |
| 14  | Hard-delete job for soft-deleted records >30d                   | ✅     | `LeadHardDeleteJob` — `@Scheduled` hourly purge, retention days configurable via `app.retention.deleted-leads-days`.                                                                                                                    |

## 🟡 Strongly recommended for launch

| #   | Feature                                      | Status | Notes                                                                                                                                                                                                                                                                                                                                     |
| --- | -------------------------------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 15  | Stripe Billing / subscriptions               | ✅     | `BillingService` (stub + real). `GET/POST /api/billing` + Checkout + Customer Portal + `/webhooks/stripe` with HMAC verify. Set `BILLING_MODE=real` + Stripe keys to enable live.                                                                                                                                                         |
| 16  | Plan-based feature gating                    | ✅     | `BillingGate` + `PlanLimits`. UI/manual creates 402 over quota; webhooks accept + audit. AI requires Pro. Usage bar in /billing.                                                                                                                                                                                                          |
| 17  | Onboarding wizard (3 steps after signup)     | ✅     | `/onboarding` page (WhatsApp → auto-reply → sample lead). Signup redirects here. `POST /api/settings/complete-onboarding` records completion.                                                                                                                                                                                             |
| 18  | Lead assignment + "assigned to me" filter    | ✅     | `assigned_to_user_id` FK + `PATCH /api/leads/{id}/assign`. Query params `?mine=true` and `?assignedToUserId`. UI: assignee dropdown on detail; "Mine" toggle on list.                                                                                                                                                                     |
| 19  | Notes on leads (internal-only)               | ✅     | `lead_notes` table + `/api/leads/{id}/notes` (GET, POST, DELETE). Author tracked via JWT. UI: Internal notes card on lead detail page.                                                                                                                                                                                                    |
| 20  | Tags / labels on leads                       | ✅     | `tags` + `lead_tags` M2M. `/api/tags` catalog and `/api/leads/{id}/tags` (attach/detach). Inline tag chips with combobox-style add on lead detail.                                                                                                                                                                                        |
| 21  | Bulk actions (status change, delete)         | ✅     | `POST /api/leads/bulk-delete` + `POST /api/leads/bulk-status` (skip non-tenant ids). UI: checkbox column on leads list + sticky action bar (set-status select + delete).                                                                                                                                                                  |
| 22  | WhatsApp template message support            | ✅     | `POST /api/leads/{id}/messages/template` — Meta template payload via `WhatsAppClient.sendTemplate`. Upstream errors mapped to 502 by `ApiExceptionHandler`.                                                                                                                                                                               |
| 23  | Real-time conversation view (SSE or polling) | ✅     | `MessageEventBus` (Reactor Sinks) + `GET /api/leads/{id}/messages/stream` SSE. JWT via `?access_token=` query param (EventSource limitation). Frontend wires `EventSource` for live messages.                                                                                                                                             |
| 24  | Full-text search on leads + messages         | ✅     | `LeadRepository.search` (LOWER + LIKE across name/phone/email/message; portable to H2). Frontend debounces input → sends `?q=`. Switch to `tsvector + GIN` later for >100k leads/tenant.                                                                                                                                                  |
| 25  | Dashboard analytics charts                   | ✅     | 3 endpoints under `/api/analytics`. Dashboard shows KPI cards + 30-day line chart + funnel bars + source bars. Built with `recharts`.                                                                                                                                                                                                     |
| 26  | Email template editor (multi-template)       | ✅     | `message_templates` table + `/api/templates` CRUD. Per (channel, event) default flag. `TemplateRenderer` substitutes `{{name}}` / `{{phone}}` / `{{email}}` / `{{source}}` / `{{business}}`. `MessagingService.sendAutoReply` now resolves: DB default → `tenant.autoReplyTemplate` → hard-coded. New /templates page with dialog editor. |
| 27  | 2FA (TOTP) for ADMIN/OWNER                   | ✅     | `dev.samstevens.totp`. `/api/2fa/{enroll,confirm,disable,status}`. Login returns `twoFactorRequired:true` when enabled. Secrets AES-GCM encrypted. QR enrollment in /profile.                                                                                                                                                             |
| 28  | Audit log filters + export                   | ✅     | `/api/audit` now accepts `entityType`, `entityId`, `action`, `actor` (substring), `from`, `to` filters via JPA Specification. `/api/audit/export` streams CSV with the same filters. UI: filter inputs + Export button on /audit page.                                                                                                    |
| 29  | Outbound webhooks for customer integrations  | ✅     | `webhook_subscriptions` table, `/api/webhook-subscriptions` CRUD, HMAC-SHA256 signing (`X-Crm-Signature-256`), auto-disable after 10 consecutive failures. Fires on `lead.created`.                                                                                                                                                       |
| 30  | Public REST API + per-user API keys          | ✅     | `/v1/leads` GET/POST/PATCH via `ApiKeyAuthFilter` (Bearer `crm_...`). `/api/api-keys` CRUD, SHA-256 stored, cleartext shown once. JWT filter skips `/v1/**`.                                                                                                                                                                              |

## 🟢 Nice-to-have post-launch

| #   | Feature                              | Status | Notes                                                                                                                                                                                                                                                                          |
| --- | ------------------------------------ | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 31  | Mobile-responsive QA pass            | ⬜     | Tailwind already responsive; verify on real devices.                                                                                                                                                                                                                           |
| 32  | Mobile push notifications            | ⬜     | Web Push API or native wrapper.                                                                                                                                                                                                                                                |
| 33  | Instagram DM + Facebook Messenger    | ⬜     | Same Meta Graph API.                                                                                                                                                                                                                                                           |
| 34  | Calendar / appointment booking       | ✅     | `appointments` table + per-tenant `booking_slug` + public `GET/POST /book/{slug}` (no auth) creates Lead + Appointment. Settings UI exposes the slug. Detail page lists scheduled appointments.                                                                                |
| 35  | RAG knowledge base for AI replies    | ✅     | `knowledge_documents` (embedding stored as JSON text — portable; swap to pgvector when corpus grows). OpenAI `text-embedding-3-small`. `KnowledgeService.retrieve` returns top-k by cosine. `AiReplyService` injects context into the system prompt. /knowledge page for CRUD. |
| 36  | Team chat / @mentions                | ⬜     |                                                                                                                                                                                                                                                                                |
| 37  | i18n (EN / ZH / MS)                  | ⬜     | next-intl.                                                                                                                                                                                                                                                                     |
| 38  | Dark mode                            | ⬜     | Tailwind dark variant.                                                                                                                                                                                                                                                         |
| 39  | Admin "impersonate user" for support | ⬜     | Audit-logged.                                                                                                                                                                                                                                                                  |
| 40  | In-app changelog / what's new        | ⬜     | Markdown-driven.                                                                                                                                                                                                                                                               |

## 🛠️ Engineering / DevOps before scaling

| #   | Feature                                        | Status | Notes                                                                                                                                                                                                                                       |
| --- | ---------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 41  | CI/CD (GitHub Actions: test → image → deploy)  | ✅     | `.github/workflows/ci.yml` runs Maven tests + Next.js build + Docker builds on PR/push. `release.yml` pushes both images to GHCR on `v*` tag.                                                                                               |
| 42  | Tenant isolation integration tests             | ✅     | `TenantIsolationIntegrationTest` 13 cases; caught & fixed real cross-tenant leak.                                                                                                                                                           |
| 43  | DB migration rollback strategy                 | ✅     | `src/main/resources/db/undo/U{n}__*.sql` convention + `deploy/scripts/rollback.sh`. `src/main/resources/db/README.md` documents the workflow and when (not) to roll back. Created U16/U17 examples.                                         |
| 44  | Secrets manager (Doppler / AWS SM / Vault)     | ✅     | `deploy/scripts/entrypoint.sh` pulls from `doppler` / `aws-secrets` / `vault` / `sops-file` based on `SECRETS_PROVIDER`. No-op when unset (backward compatible). DEPLOY.md §8 documents SOPS+age recipe.                                    |
| 45  | Container image scanning (Trivy in CI)         | ✅     | `aquasecurity/trivy-action` runs on both built images + repo filesystem in `ci.yml`. HIGH/CRITICAL fail the build (repo scan informational on first runs).                                                                                  |
| 46  | Dependency vulnerability scanning (Dependabot) | ✅     | `.github/dependabot.yml` covers Maven (grouped: spring-boot, stripe, google), npm (grouped: next, radix), GitHub Actions, and Docker base images.                                                                                           |
| 47  | Staging environment                            | ⬜     | Mirror of prod.                                                                                                                                                                                                                             |
| 48  | Monitoring (Prometheus + Grafana)              | ✅     | `micrometer-registry-prometheus` exposes `/actuator/prometheus`. Prod compose adds `prometheus` + `grafana` services; provisioned datasource and starter dashboard (HTTP rate, p95, JVM memory, Hikari pool, 5xx rate). Grafana on `:3001`. |
| 49  | Uptime monitoring                              | ✅     | `/actuator/health/{db,redis,liveness,readiness}` exposed publicly. Custom Redis PING indicator. DEPLOY.md adds BetterStack/UptimeRobot recipes.                                                                                             |
| 50  | Log aggregation (Loki / Papertrail)            | ⬜     |                                                                                                                                                                                                                                             |
| 51  | DB read replica                                | ⏭     | Only when load demands.                                                                                                                                                                                                                     |
| 52  | Kafka / RabbitMQ for async events              | ⏭     | Stay on Spring events until scale demands.                                                                                                                                                                                                  |

---

## Recently completed

- **#9 + #12 — Error tracking + legal pages** (2026-06-10): Sentry SDK on backend (`sentry-spring-boot-starter-jakarta` + `sentry-logback`) and frontend (`@sentry/nextjs`), inert until `SENTRY_DSN` is set so dev is unaffected. `SentryConfig.BeforeSendCallback` strips request bodies + cookies and tags events with `tenant_id`. Public `/legal/terms` and `/legal/privacy` pages with company-name placeholders. `CookieBanner` (essential vs accept-all, localStorage-backed). Signup form now requires Terms + Privacy acceptance to enable submit. Footer links from login. Also synced the must-have table status icons with reality (#2/#4/#5/#6/#7/#8/#11 were already done but still showed ⬜). 29/29 tests still pass; frontend builds clean.
- **#10 + #14 + #22 + #29 + #30 — Backups, retention, templates, webhooks, public API** (2026-06-09): nightly S3 backups via `deploy/backup/` cron container in `docker-compose.prod.yml` (configurable cron + retention); hourly `LeadHardDeleteJob` purges soft-deleted leads older than `app.retention.deleted-leads-days` (default 30); WhatsApp pre-approved template send at `POST /api/leads/{id}/messages/template`; outbound webhooks with `webhook_subscriptions` table, HMAC-SHA256 signing (`X-Crm-Signature-256`, Stripe-style), wildcard event filters (`lead.*`), auto-disable after 10 failures, fires on `lead.created`; public REST API at `/v1/leads` (GET/POST/PATCH) authenticated by per-user API keys (`crm_...` Bearer), SHA-256-hashed at rest, cleartext shown once. New `ApiExceptionHandler` maps `WebClient` upstream failures to 502 instead of 500 stack traces. JWT filter now skips `/v1/**` so API-key tokens aren't rejected as malformed JWTs. `@EnableAsync` + `@EnableScheduling` added. V18 + V19 Flyway migrations with matching undo scripts. 29/29 tests pass; webhook + API key flows live-verified.
- **#16 + #18 + #27 + #49 — Limits, assignment, 2FA, uptime** (2026-06-08): plan-based gating (lead quota + AI gating, billing usage bar in UI), per-user lead assignment (`PATCH /api/leads/{id}/assign`, "Mine" filter, picker on detail page), TOTP 2FA with QR enrollment + login challenge (encrypted at rest), enriched actuator health with per-component endpoints + monitor docs. 29/29 tests pass.
- **#6 + #7 + #8 — Auth hardening** (2026-06-08): per-user lockout (5 failures → 15-minute lock), per-IP + per-email Redis rate limit on `/auth/login` (default 10/min, returns 429), email verification via signed token sent through SMTP (SES-compatible). New endpoints: `POST /auth/verify-email`, `POST /auth/resend-verification`. New `/verify-email` page in dashboard. `EMAIL_VERIFICATION_REQUIRED` env flag (default off in dev, on in prod). Plus role hierarchy fix — OWNER now satisfies any ADMIN check via `RoleHierarchyImpl`. 26/26 tests pass.
- **#2 — Self-service signup + shadcn/ui migration** (2026-06-08): `POST /auth/register` creates tenant + OWNER user with JWT (`SignupIntegrationTest` 3/3). Frontend fully migrated to shadcn pattern: Tailwind theme tokens (CSS vars), 10 UI primitives (Button, Input, Label, Card, Badge, Table, Dialog, Select, Checkbox, Textarea), all 8 pages refactored, sidebar uses lucide-react icons. New `/signup` page links from `/login`. 22/22 tests pass.
- **#4 + #5 — Per-tenant credentials + AES-GCM encryption** (2026-06-08): WhatsApp/Telegram/OpenAI tokens now stored encrypted per tenant. Settings UI lets admins paste credentials (write-only); API returns `*Configured` booleans only. Real integration clients resolve the active tenant's creds per call, falling back to env. 7 new tests (5 cipher, 2 settings isolation). 19/19 tests pass.
- **#42 — Tenant isolation tests + isolation hardening** (2026-06-08): 11 MockMvc cases verify A/B isolation across leads, users, settings, audit, messages. Exposed and fixed a real bug — `findAll()` queries weren't tenant-scoped because the `@Filter` interceptor didn't survive into service-layer transactions. Now all queries use explicit `findAllByTenantId*` methods. `LeadService.get()` also adds an explicit tenant check (Hibernate `@Filter` does not apply to find-by-PK).
- **#1 — User management** (2026-06-08): backend `/api/users` CRUD + role enum + disable + own/admin password change + last-login tracking; frontend `/users` and `/profile` pages.
- **Phase 3** (2026-06-04): Meta inbound adapter, audit log + soft delete, CSV import/export, SMTP email, Google Sheets export, Nginx prod compose + Let's Encrypt.
- **Phase 2**: webhook rate limit, WhatsApp signature verification, settings API, stats API, reply endpoint, Next.js dashboard.
- **Phase 1**: monolith scaffold, multi-tenant data model, JWT auth, stub/real integration pattern, Spring events for async flow.

## Next recommended

**🟢 All 14 🔴 Must-have items are now complete — the platform is launch-ready for paying customers.**

Remaining open items (none are launch blockers):

1. **#47 Staging environment** — spin up a second prod-shaped stack on a separate subdomain for pre-release validation.
2. **#50 Log aggregation (Loki / Papertrail)** — ship container logs off-host so they survive restarts.
3. **#31 Mobile-responsive QA pass** — walk each dashboard page on a real phone before announcing.
4. **#33 Instagram DM + Messenger** — reuses the Meta Graph adapter.
5. **#38 Dark mode** — Tailwind already wired, one CSS-vars sweep.
