global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: crm-app
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - app:8080
        labels:
          service: crm-app
